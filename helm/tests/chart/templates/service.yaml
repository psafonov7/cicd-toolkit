apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
  labels:
    name: {{ .Values.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    name: {{ .Values.name }}
