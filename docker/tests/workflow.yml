on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: cruizba/ubuntu-dind:latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./docker
        with:
          build_number: "1.0.0"
          context_path: ./docker/tests
